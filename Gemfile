source "https://rubygems.org"

# Pins Jekyll + plugin versions to exactly what GitHub Pages runs in
# production, so `bundle exec jekyll serve` locally matches the live build.
gem "github-pages", group: :jekyll_plugins